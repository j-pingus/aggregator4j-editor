import { FilterControlsPipe } from './filter-controls.pipe';

describe('FilterControlsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterControlsPipe();
    expect(pipe).toBeTruthy();
  });
});
